diff --git a/onnxruntime/contrib_ops/cuda/bert/linear_attention_impl.cu b/onnxruntime/contrib_ops/cuda/bert/linear_attention_impl.cu
index b27641c..88fb385 100644
--- a/onnxruntime/contrib_ops/cuda/bert/linear_attention_impl.cu
+++ b/onnxruntime/contrib_ops/cuda/bert/linear_attention_impl.cu
@@ -981,6 +981,66 @@ __global__ void LinearAttentionDecodeColKernel(

 }  // anonymous namespace

+template <typename T, int DK, int DV>
+static Status LaunchFixedShapeKernel(
+    dim3 grid, cudaStream_t stream,
+    const T* query, const T* key, const T* value, T* present_state,
+    const T* decay, const T* beta, T* output,
+    int seq_len, int q_num_heads, int kv_num_heads, int n_k_heads,
+    int output_hidden, float scale,
+    bool needs_decay, bool decay_per_key_dim, bool needs_beta, bool beta_per_head, bool needs_retrieval) {
+  constexpr int max_dim = (DK > DV) ? DK : DV;
+  const size_t fixed_smem_size = (static_cast<size_t>(DK) * DV + DK + max_dim) * sizeof(float);
+  const dim3 fixed_block(max_dim, 1, 1);
+
+  if (fixed_smem_size > 48 * 1024) {
+    cudaError_t attr_err = cudaFuncSetAttribute(
+        LinearAttentionRecurrentKernelFixedShape<T, DK, DV>,
+        cudaFuncAttributeMaxDynamicSharedMemorySize,
+        static_cast<int>(fixed_smem_size));
+    if (attr_err != cudaSuccess) {
+      return CUDA_CALL(attr_err);
+    }
+  }
+
+  LinearAttentionRecurrentKernelFixedShape<T, DK, DV><<<grid, fixed_block, fixed_smem_size, stream>>>(
+      query, key, value, present_state, decay, beta, output,
+      seq_len, q_num_heads, kv_num_heads, n_k_heads, output_hidden, scale,
+      needs_decay, decay_per_key_dim, needs_beta, beta_per_head, needs_retrieval);
+
+  return CUDA_CALL(cudaGetLastError());
+}
+
+template <typename T, int DK>
+static Status LaunchFixedShapeDecodeKernel(
+    dim3 decode_grid, dim3 decode_block, cudaStream_t stream,
+    const T* query, const T* key, const T* value, T* present_state,
+    const T* decay, const T* beta, T* output,
+    int seq_len, int q_num_heads, int kv_num_heads, int n_k_heads,
+    int d_v, int output_hidden, float scale,
+    bool needs_decay, bool decay_per_key_dim, bool needs_beta, bool beta_per_head, bool needs_retrieval) {
+  LinearAttentionDecodeKernel<T, DK><<<decode_grid, decode_block, 0, stream>>>(
+      query, key, value, present_state, decay, beta, output,
+      seq_len, q_num_heads, kv_num_heads, n_k_heads, d_v, output_hidden, scale,
+      needs_decay, decay_per_key_dim, needs_beta, beta_per_head, needs_retrieval);
+  return CUDA_CALL(cudaGetLastError());
+}
+
+template <typename T, int DK>
+static Status LaunchFixedShapeDecodeColKernel(
+    dim3 decode_grid, dim3 decode_block, cudaStream_t stream,
+    const T* query, const T* key, const T* value, T* present_state,
+    const T* decay, const T* beta, T* output,
+    int seq_len, int q_num_heads, int kv_num_heads, int n_k_heads,
+    int d_v, int output_hidden, float scale,
+    bool needs_decay, bool decay_per_key_dim, bool needs_beta, bool beta_per_head, bool needs_retrieval) {
+  LinearAttentionDecodeColKernel<T, DK><<<decode_grid, decode_block, 0, stream>>>(
+      query, key, value, present_state, decay, beta, output,
+      seq_len, q_num_heads, kv_num_heads, n_k_heads, d_v, output_hidden, scale,
+      needs_decay, decay_per_key_dim, needs_beta, beta_per_head, needs_retrieval);
+  return CUDA_CALL(cudaGetLastError());
+}
+
 template <typename T>
 Status LaunchLinearAttentionKernel(
     cudaStream_t stream,
@@ -1032,19 +1092,16 @@ Status LaunchLinearAttentionKernel(
                              (d_v + kColsPerBlock - 1) / kColsPerBlock);
       const dim3 decode_block(kColsPerBlock, 1, 1);

-      auto launch_col = [&](auto dk_tag) -> Status {
-        constexpr int DK = decltype(dk_tag)::value;
-        LinearAttentionDecodeColKernel<T, DK><<<decode_grid, decode_block, 0, stream>>>(
-            query, key, value, present_state, decay, beta, output,
+      if (d_k == 64) {
+        return LaunchFixedShapeDecodeColKernel<T, 64>(
+            decode_grid, decode_block, stream, query, key, value, present_state, decay, beta, output,
             seq_len, q_num_heads, kv_num_heads, n_k_heads, d_v, output_hidden, scale,
             needs_decay, decay_per_key_dim, needs_beta, beta_per_head, needs_retrieval);
-        return CUDA_CALL(cudaGetLastError());
-      };
-
-      if (d_k == 64) {
-        return launch_col(std::integral_constant<int, 64>{});
       } else if (d_k == 128) {
-        return launch_col(std::integral_constant<int, 128>{});
+        return LaunchFixedShapeDecodeColKernel<T, 128>(
+            decode_grid, decode_block, stream, query, key, value, present_state, decay, beta, output,
+            seq_len, q_num_heads, kv_num_heads, n_k_heads, d_v, output_hidden, scale,
+            needs_decay, decay_per_key_dim, needs_beta, beta_per_head, needs_retrieval);
       }
     }

@@ -1052,62 +1109,48 @@ Status LaunchLinearAttentionKernel(
                            (d_v + kWarpsPerBlock - 1) / kWarpsPerBlock);
     const dim3 decode_block(32, kWarpsPerBlock, 1);

-    auto launch_decode = [&](auto dk_tag) -> Status {
-      constexpr int DK = decltype(dk_tag)::value;
-      LinearAttentionDecodeKernel<T, DK><<<decode_grid, decode_block, 0, stream>>>(
-          query, key, value, present_state, decay, beta, output,
+    if (d_k == 64) {
+      return LaunchFixedShapeDecodeKernel<T, 64>(
+          decode_grid, decode_block, stream, query, key, value, present_state, decay, beta, output,
           seq_len, q_num_heads, kv_num_heads, n_k_heads, d_v, output_hidden, scale,
           needs_decay, decay_per_key_dim, needs_beta, beta_per_head, needs_retrieval);
-      return CUDA_CALL(cudaGetLastError());
-    };
-
-    if (d_k == 64) {
-      return launch_decode(std::integral_constant<int, 64>{});
     } else if (d_k == 128) {
-      return launch_decode(std::integral_constant<int, 128>{});
+      return LaunchFixedShapeDecodeKernel<T, 128>(
+          decode_grid, decode_block, stream, query, key, value, present_state, decay, beta, output,
+          seq_len, q_num_heads, kv_num_heads, n_k_heads, d_v, output_hidden, scale,
+          needs_decay, decay_per_key_dim, needs_beta, beta_per_head, needs_retrieval);
     } else {  // d_k == 256
-      return launch_decode(std::integral_constant<int, 256>{});
+      return LaunchFixedShapeDecodeKernel<T, 256>(
+          decode_grid, decode_block, stream, query, key, value, present_state, decay, beta, output,
+          seq_len, q_num_heads, kv_num_heads, n_k_heads, d_v, output_hidden, scale,
+          needs_decay, decay_per_key_dim, needs_beta, beta_per_head, needs_retrieval);
     }
   }

-  auto launch_fixed = [&](auto dk_tag, auto dv_tag) -> Status {
-    constexpr int DK = decltype(dk_tag)::value;
-    constexpr int DV = decltype(dv_tag)::value;
-    constexpr int max_dim = (DK > DV) ? DK : DV;
-    // Layout: S_smem[DK*DV] + k_buf[DK] + s_scratch[max(DK,DV)]
-    const size_t fixed_smem_size = (static_cast<size_t>(DK) * DV + DK + max_dim) * sizeof(float);
-    const dim3 fixed_block(max_dim, 1, 1);
-
-    if (fixed_smem_size > 48 * 1024) {
-      cudaError_t attr_err = cudaFuncSetAttribute(
-          LinearAttentionRecurrentKernelFixedShape<T, DK, DV>,
-          cudaFuncAttributeMaxDynamicSharedMemorySize,
-          static_cast<int>(fixed_smem_size));
-      if (attr_err != cudaSuccess) {
-        return CUDA_CALL(attr_err);
-      }
-    }
-
-    LinearAttentionRecurrentKernelFixedShape<T, DK, DV><<<grid, fixed_block, fixed_smem_size, stream>>>(
-        query, key, value, present_state, decay, beta, output,
-        seq_len, q_num_heads, kv_num_heads, n_k_heads, output_hidden, scale,
-        needs_decay, decay_per_key_dim, needs_beta, beta_per_head, needs_retrieval);
-
-    return CUDA_CALL(cudaGetLastError());
-  };
-
   // Fast paths for common (d_k, d_v) pairs
   if (d_k == 64 && d_v == 64 && max_threads_per_block >= 64) {
-    return launch_fixed(std::integral_constant<int, 64>{}, std::integral_constant<int, 64>{});
+    return LaunchFixedShapeKernel<T, 64, 64>(
+        grid, stream, query, key, value, present_state, decay, beta, output,
+        seq_len, q_num_heads, kv_num_heads, n_k_heads, output_hidden, scale,
+        needs_decay, decay_per_key_dim, needs_beta, beta_per_head, needs_retrieval);
   }
   if (d_k == 128 && d_v == 128 && max_threads_per_block >= 128) {
-    return launch_fixed(std::integral_constant<int, 128>{}, std::integral_constant<int, 128>{});
+    return LaunchFixedShapeKernel<T, 128, 128>(
+        grid, stream, query, key, value, present_state, decay, beta, output,
+        seq_len, q_num_heads, kv_num_heads, n_k_heads, output_hidden, scale,
+        needs_decay, decay_per_key_dim, needs_beta, beta_per_head, needs_retrieval);
   }
   if (d_k == 128 && d_v == 64 && max_threads_per_block >= 128) {
-    return launch_fixed(std::integral_constant<int, 128>{}, std::integral_constant<int, 64>{});
+    return LaunchFixedShapeKernel<T, 128, 64>(
+        grid, stream, query, key, value, present_state, decay, beta, output,
+        seq_len, q_num_heads, kv_num_heads, n_k_heads, output_hidden, scale,
+        needs_decay, decay_per_key_dim, needs_beta, beta_per_head, needs_retrieval);
   }
   if (d_k == 64 && d_v == 128 && max_threads_per_block >= 128) {
-    return launch_fixed(std::integral_constant<int, 64>{}, std::integral_constant<int, 128>{});
+    return LaunchFixedShapeKernel<T, 64, 128>(
+        grid, stream, query, key, value, present_state, decay, beta, output,
+        seq_len, q_num_heads, kv_num_heads, n_k_heads, output_hidden, scale,
+        needs_decay, decay_per_key_dim, needs_beta, beta_per_head, needs_retrieval);
   }

   // Generic fallback
diff --git a/onnxruntime/contrib_ops/cuda/bert/xqa/utils.cuh b/onnxruntime/contrib_ops/cuda/bert/xqa/utils.cuh
index 3335707..5a0ddbd 100644
--- a/onnxruntime/contrib_ops/cuda/bert/xqa/utils.cuh
+++ b/onnxruntime/contrib_ops/cuda/bert/xqa/utils.cuh
@@ -203,11 +203,8 @@ __device__ __host__ inline Vec<Dst, size> convert(Vec<Src, size> const& src) {
       dst[size - 1] = Dst{src[size - 1]};
     }
   } else if constexpr (mha::is_same_v<Src, __nv_fp8_e4m3> && mha::is_same_v<Dst, float>) {
-    for (uint32_t i = 0; i < size - 1; i += 2) {
-      reinterpret_cast<float2&>(dst[i]) = float2(reinterpret_cast<__nv_fp8x2_e4m3 const&>(src[i]));
-    }
-    if constexpr (size % 2 != 0) {
-      dst[size - 1] = Dst{src[size - 1]};
+    for (uint32_t i = 0; i < size; i++) {
+      dst[i] = Dst{src[i]};
     }
   } else if constexpr (mha::is_same_v<Src, float> && mha::is_same_v<Dst, __nv_fp8_e4m3>) {
     for (uint32_t i = 0; i < size - 1; i += 2) {
diff --git a/onnxruntime/core/providers/cuda/llm/attention.cc b/onnxruntime/core/providers/cuda/llm/attention.cc
index dc53b02..7fba703 100644
--- a/onnxruntime/core/providers/cuda/llm/attention.cc
+++ b/onnxruntime/core/providers/cuda/llm/attention.cc
@@ -34,7 +34,7 @@ namespace llm_attention_detail {
 template <typename T>
 float MaskedBiasSentinel() {
   return std::max(static_cast<float>(std::numeric_limits<T>::lowest()),
-                  ::onnxruntime::contrib::cuda::kCutlassSafeMaskFilterValue);
+                  -1.0e+30f);
 }

 template <typename NodeType>
